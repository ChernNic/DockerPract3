# Используем последний стабильный образ Dart.
FROM dart:stable

# Создаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл calc.dart в рабочую директорию контейнера
COPY calc.dart /app

# Компилируем приложение (опционально, можно запустить напрямую)
RUN dart compile exe calc.dart -o calc

# Указываем команду для запуска приложения
CMD ["./calc"]
