# Используем официальный образ Nginx
FROM nginx:latest

# Копируем все HTML-файлы из текущей директории в стандартную папку Nginx для веб-файлов
COPY . /usr/share/nginx/html

# Указываем порт, который будет открыт для доступа
EXPOSE 80
